# Copie para .env — NUNCA commite o .env real.
# Em desenvolvimento local, deixe vazio para usar o proxy (package.json → localhost:8080).
# Em produção (Vercel), defina a URL pública da API.

REACT_APP_API_URL=
